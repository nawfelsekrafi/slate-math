import { PlatePluginOptions } from "@udecode/plate-core";

export const ELEMENT_EQUATIONBOX = 'equationbox';

export const DEFAULTS_EQUATIONBOX: Partial<PlatePluginOptions> = {
  hotkey: 'mod+shift+,',
};
